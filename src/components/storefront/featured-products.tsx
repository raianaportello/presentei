import Image from "next/image";
import Link from "next/link";
import { Container } from "@/components/ui/container";
import type { ProductCardData } from "@/modules/catalog/types";
import { formatMoney } from "@/modules/pricing/format-money";

export function FeaturedProducts({ products }: { products: ProductCardData[] }) { return <section className="bg-white py-20 sm:py-28"><Container><div className="flex items-end justify-between gap-6"><div><p className="text-xs font-black uppercase tracking-[.2em] text-[var(--brand-orange-deep)]">Escolha seu começo</p><h2 className="font-display mt-4 text-5xl font-black tracking-[-.06em]">Canecas que pedem uma ideia.</h2></div><Link href="/produtos" className="hidden text-sm font-black underline underline-offset-4 sm:block">Ver catálogo</Link></div><div className="mt-10 grid gap-6 md:grid-cols-3">{products.slice(0,3).map((product) => <article key={product.id} className="group"><Link href={`/produtos/${product.slug}`} className="block overflow-hidden rounded-[var(--radius-md)] bg-[var(--brand-surface)] focus-visible:outline-3 focus-visible:outline-[var(--brand-orange)]"><Image src={product.imageUrl} alt={product.imageAlt} width={640} height={640} className="aspect-square w-full transition-transform duration-500 group-hover:scale-[1.03]" /></Link><div className="mt-5 flex items-start justify-between gap-4"><div><h3 className="font-display text-2xl font-black">{product.name}</h3><p className="mt-1 text-sm text-[var(--brand-muted)]">Personalizável</p></div><p className="font-black">{formatMoney(product.priceCents)}</p></div><Link href={`/personalizar?produto=${product.slug}`} className="mt-4 inline-flex text-sm font-black text-[var(--brand-orange-deep)] underline underline-offset-5">Personalizar →</Link></article>)}</div></Container></section>; }
